{
    "tasks": [
        {
            "type": "shell",
            "label": "Build OpenGL viewer",
            "command": "make",
            "args": [
                "all"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Builds the OpenGL GLFW sample with the Makefile."
        }
    ],
    "version": "2.0.0"
}
